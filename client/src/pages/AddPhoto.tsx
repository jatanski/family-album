import React, { FC } from 'react';
import Main from '../components/AddPhoto/AddPhoto';

const Albums: FC = () => (
	<section className="addphoto">
		<Main></Main>
	</section>
);

export default Albums;
